services:
  - type: web
    name: telegram-ai-bot
    env: python
    plan: free
    branch: main
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      - key: TELEGRAM_TOKEN
        # مقدار توکن را از اینجا حذف کنید